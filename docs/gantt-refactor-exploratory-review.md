# Gantt: рефакторинг, дослідницьке тестування та UX-аудит

Дата: 7 вересня 2026. Обсяг погоджений користувачем: алгоритми фільтрації та сортування зі
збереженням поведінки; загальні перевірки та дослідницьке тестування; UX-зміни — лише
рекомендації.

## Зміни

У `packages/domain/src/filters.ts`:

- Побудова груп дочірніх завдань через додавання до локального масиву замість копіювання
  всієї групи при кожному додаванні.
- Один `Intl.Collator` на сортування; спільне порівняння порожніх значень, напрямку й
  однакових ключів. Збережено порядок категорій статусу, порівняння статусів із
  початковими пробілами та зворотний порядок однакових ключів у descending.
- Індекс нащадків створюється тільки коли `includeDescendants` активний. Прямі збіги
  збираються без проміжного масиву.
- Спільні предки обходяться один раз за виклик. Захист від циклів і порядок множин
  збережені.
- Черга нащадків обходиться індексом замість `shift()`.

Публічний API, UI, мережеві запити й дані Jira не змінені. Додано три регресійні тести в
`filters.regression.test.ts`. Наявні тести не редагувалися.

## Перевірка еквівалентності та продуктивності

Окремий тимчасовий Node.js-скрипт порівняв код до та після змін: 100 наборів по 40
завдань, випадкові й широкі ієрархії, цикли/відсутні батьки, Unicode, пробіли й порожні
назви. Сім ключів сортування × два напрямки; фільтрація з AND/OR, пошуком, нащадками й
excludeDone. Усі 3000 порівнянь пройшли, вхідні дані не мутували. Генератор: xorshift32,
seed 42.

Мікробенчмарк: 1000 завдань, 5 прогрівів, медіана 15 запусків, один Node.js-процес. Фільтр
шукає `P-` та включає нащадків. Wide — один батько та 999 дітей; chain — ланцюг із 1000
завдань.

| Операція                              | До, мс | Після, мс | Прискорення |
| ------------------------------------- | -----: | --------: | ----------: |
| Сортування широкої ієрархії за назвою | 10.147 |     0.642 |       15.8× |
| Фільтрація широкої ієрархії           |  0.978 |     0.201 |        4.9× |
| Сортування ланцюга                    |  0.172 |     0.142 |        1.2× |
| Фільтрація ланцюга                    | 30.508 |     0.304 |      100.4× |

Це локальні виміри алгоритмів, не швидкість усього застосунку чи Jira API. Ланцюг —
штучний стрес-сценарій. Виграш залежить від структури дошки, ключа сортування й runtime.

## Автоматичні перевірки

| Перевірка                                        | Результат                                                                 |
| ------------------------------------------------ | ------------------------------------------------------------------------- |
| Усі наявні Vitest-тести                          | 377/377, 60 файлів, успішно                                               |
| Нові регресійні тести                            | 3/3, успішно                                                              |
| TypeScript: root, обидва apps і всі packages     | Успішно, 10 конфігурацій                                                  |
| ESLint зміненого коду й нових тестів             | Успішно, без попереджень                                                  |
| Форматування зміненого коду й нових тестів       | Успішно                                                                   |
| Web, extension, content-script production builds | Успішно                                                                   |
| Валідація та ZIP                                 | 10 entry points перевірено; створено `dist/power-view-for-jira-0.3.0.zip` |
| Загальний ESLint до змін                         | 11 помилок і 7 попереджень у файлах поза цим рефакторингом                |
| Загальний Prettier після змін                    | 10 незмінених файлів потребують форматування                              |
| Наявні Playwright E2E                            | 2/2 не пройшли; причини нижче                                             |

Команди виконані напряму через локальні `node_modules/.bin` з bundled Node у PATH:
доступний pnpm-wrapper намагався звертатися до registry та перевстановлювати modules ще до
запуску scripts. Залежності не оновлювалися.

У початковому повному прогоні був один timeout в App.test.tsx. Окремий повтор і повний
прогін після змін пройшли без зміни тайм-аутів. Production web build попереджає про JS
chunk 556.15 kB (152.19 kB gzip).

E2E після дозволеного запуску Chromium поза sandbox дійшли до застосунку, але очікують
старі заголовки `Open Power View from Jira.` і `Test your Jira connection.`. Фактично
відображається `Configure your Jira workspace`. Другий сценарій уже показує
`Connected as Fixture Cloud User`, але fixture не відповідає на запит дошок і показує
помилку їх завантаження. Тести не доводять проходження наскрізних сценаріїв; їх потрібно
адаптувати до поточного setup-flow й доповнити fixture. Не слід просто замінювати
заголовки без перевірки всього сценарію.

## Дослідницькі сценарії в браузері

Середовище: локальний Vite, Codex in-app browser; штатний desktop viewport, 768×900 і
390×844. Використані лише наявні preview-дані, жодних записів у реальний Jira. Розрізняємо
попередній `GanttView` (`?gantt-preview`) і поточний `GanttV2` (`?app-preview` → Gantt).

| Область / сценарій                         | Спостереження                                                                                              |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| GanttView: початковий розклад              | 6 завдань, 2 видимі кореневі рядки                                                                         |
| Розгортання двох рівнів                    | Послідовно 4 та 6 рядків, вкладення збережене                                                              |
| Пошук POWER-114                            | 1 збіг, 3 рядки, 2 контекстні предки                                                                       |
| Вибір результату                           | Деталі POWER-114, дати, assignee й залежність POWER-113 правильні                                          |
| Пошук без збігів                           | Пояснення відсутності результатів, пропозиція скинути/розширити фільтри                                    |
| Reset filters                              | Усі 6 розгорнутих рядків повернулися                                                                       |
| Сортування Summary                         | Сортування серед siblings, батьки залишаються перед дітьми                                                 |
| Week → Month                               | Шкала перейшла до місяців, вибір завдання збережений                                                       |
| Risk: Blocked                              | 2 збіги, 3 рядки, 1 контекстний предок                                                                     |
| Escape у Risk                              | Меню залишається відкритим — UX-недолік                                                                    |
| POWER-112 + Include descendants            | 1 збіг, 4 рядки, включено дітей та предка                                                                  |
| Include descendants + Hide completed       | Завершений нащадок залишається: hide застосовується до прямих збігів; поточна поведінка збережена          |
| Велика дошка                               | 1000 завдань; aria-rowcount=1001 із заголовком; 27 рядків даних у DOM                                      |
| Пошук POWER-999                            | Знайдено 1 рядок, хоча його не було в початковому DOM                                                      |
| Прокрутка великої дошки                    | Видимий діапазон змінився до POWER-83…109, DOM залишився обмеженим 27 рядками                              |
| GanttView 768 px                           | Панель фільтрів займає значну частину висоти; для шкали лишається мало ширини                              |
| GanttView 390 px                           | Перші рядки починаються приблизно на 760 px; шкала поза видимою шириною                                    |
| App preview: старт                         | Автоматичний setup доводить до вибору Reports/Gantt                                                        |
| GanttV2: сортування                        | Task name + descending переставляє LBR-2 перед LBR-1                                                       |
| GanttV2: Hide completed                    | Залишається тільки LBR-2, завершений LBR-1 приховано                                                       |
| GanttV2: редактор статусу                  | Відкривається; fixture не має transitions, показано unsupported format; успішний запис не перевірено       |
| GanttV2: Escape в редакторі                | Діалог закривається                                                                                        |
| GanttV2: стрілка вниз і Tab                | ArrowDown із першого issue-link не переходить до наступного рядка; Tab переходить на його редактор статусу |
| GanttV2: Week / Day / Today                | Керування доступне; у Week щоденні підписи перекриваються                                                  |
| GanttV2: активний Hide completed на 768 px | Фільтр продовжує діяти, але перемикач невидимий                                                            |
| Reports: Board Health                      | 2 issues, 1 open, 1 done; зрозумілий стан відсутнього активного спринту                                    |
| Reports: Weekly                            | Поля й період оновлюються                                                                                  |
| Reports: Generate                          | Preview повертає unsupported format, кнопка знову доступна; успішна генерація в браузері не підтверджена   |
| Settings → Save and continue               | Повернення до chooser; вибраний порядок сортування Gantt зберігся під час навігації                        |
| Запуск поза extension-контекстом           | Пояснення помилки контексту й інструкція повернутися в Jira                                                |

У переглянутому console log браузерної вкладки помилок JavaScript не було. Повідомлення
помилок від fixture відображалися в UI. Повний screen-reader-аудит, реальні Jira
Cloud/Data Center, 200% browser zoom, drag-and-drop із записом, rate limiting і
persistence після перезапуску браузера в цьому дослідницькому проході не перевірялися.
Частину цих сценаріїв покривають наявні component/unit-тести, що не замінює живу
інтеграцію.

## UX-рекомендації за пріоритетом

Це рекомендації, не внесені зміни. Виявлені UI-проблеми лежать у незмінених компонентах.

### P1 — Не приховувати активні фільтри на вузькому екрані

Відтворення: GanttV2 → увімкнути Hide completed на desktop → ширина 768 px. Завершені
issues лишаються прихованими, перемикач зникає через
`.gantt-v2-hide-completed { display: none }` при ≤900 px.

Рекомендація: переносити перемикач або розміщувати його в доступному меню Filters з
кількістю активних умов і дією Clear all. Критерій приймання: на будь-якій підтримуваній
ширині користувач бачить, що дані відфільтровані, і може скасувати кожну умову.

### P1 — Зробити шкалу часу читабельною

У GanttV2 Week щоденні MM-DD-підписи перекриваються вже на desktop. Рекомендація: підписи
тижнів для Week, місяців для Month; щоденні поділки можуть лишитися без тексту. Критерій:
жодного накладання підписів; поточна дата розпізнається без наведення.

### P1 — Узгодити клавіатурну модель таблиці

`role=grid` є, але ArrowDown із першого issue-link не переходить на наступний рядок.
Рекомендація: керований фокус, навігація стрілками/Home/End, окремий вхід у редактор і
повернення фокуса після закриття. Перевіряти й віртуалізовані рядки. Орієнтир:
[W3C APG Grid](https://www.w3.org/WAI/ARIA/apg/patterns/grid/).

### P2 — Віддати більше місця самому розкладу

У попередньому GanttView 9 полів фільтрації, логіка AND/OR і допоміжні написи займають
майже весь перший екран на 390 px. Рекомендація: залишити пошук та активні фільтри зверху,
решту — у розгортаній панелі; на вузьких екранах запропонувати перемикання списку й
timeline. У GanttV2 на 768 px таблиця теж забирає більшість ширини — потрібен адаптивний
розподіл колонок.

### P2 — Прибрати суперечливі та технічні підказки

GanttView показує `Confirmed` поруч з issue з inferred dates/Needs attention; без
пояснення це легко сприйняти як підтвердження дат. Назвати конкретно, що підтверджено.
`27 DOM rows rendered` перенести в diagnostics. У Reports винести опис changelog/worklog
cache і Shift-click з основного сценарію в help та дати явну дію оновлення даних.

### P2 — Єдина поведінка закриття і відновлення після помилок

Escape закриває status editor GanttV2, але не Risk у GanttView. Уніфікувати закриття та
повернення фокуса. Помилка unsupported format має називати невдалу операцію і пропонувати
Retry/Diagnostics; не трактувати її як відсутність дозволених transitions.

### P2 — Пояснити взаємодію Hide completed та Include descendants

Завершений нащадок залишається, якщо доданий як контекст/нащадок збігу. Це поточний
контракт, а не регресія. Перед зміною логіки потрібне продуктове рішення: чи hide є
глобальним виключенням, чи застосовується лише до прямих збігів. До того варто пояснювати
причину появи контекстного рядка.

## Рекомендована наступна перевірка якості

Оновити E2E до актуального setup → board → workspace → GanttV2 шляху; доповнити fixture
для boards, transitions і reporting. Потім виконати read/write сценарії на окремому
тестовому Jira-проєкті з перевіркою серверних значень. Поточний результат підтверджує
алгоритмічну еквівалентність і проходження unit/component перевірок, але не повну
готовність інтеграції до релізу.
